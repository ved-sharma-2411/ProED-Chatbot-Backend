# ProEd RAG Backend — runtime dependencies only
# Install: pip install -r requirements.txt

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Vector DB
pinecone>=3.0.0

# OpenAI — embeddings + LLM
openai>=1.30.0

# AWS S3 — BM25 cache storage
boto3>=1.34.0
